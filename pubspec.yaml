name: audio_files_demo
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.14.2
  cupertino_icons: ^0.1.2
  web_socket_channel: ^1.1.0
  connectivity: ^0.4.5+6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/audio.mp3
    - assets/audio2.mp3
    - assets/audio1.mp3